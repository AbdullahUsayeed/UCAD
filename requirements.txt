# Core — required for DXF processing
ezdxf>=1.2.0
shapely>=2.0.0

# AI provider integration — unified LLM interface for 100+ providers
litellm>=1.60.0

# Secret storage (optional — falls back to DPAPI on Windows)
keyring>=24.0.0
cryptography>=41.0.0
