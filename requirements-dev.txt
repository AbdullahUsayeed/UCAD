# Development / CI dependencies — not shipped with the addon.
# Includes everything from requirements.txt plus test + server deps.
-r requirements.txt

# Test suite
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
aiosqlite>=0.20.0

# Backend server — telemetry receiver (FastAPI + PostgreSQL)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
aiofiles>=24.0.0
